8pp_direct 0pp_header

keyword func()
{
	keyword var_local[] = {num, num, num};

	0com_single
	0com_single
	keyword keyword var_local = keyword(var_local) / keyword(var_local[num]);

	keyword (keyword var_local = num; var_local < var_local; ++var_local)
		namespace::var_global 2oo var_local[var_local];
}
