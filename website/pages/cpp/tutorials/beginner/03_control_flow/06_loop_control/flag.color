keyword var_local = keyword;
keyword (keyword var_local = num; var_local < var_local; ++var_local)
{
	keyword (var_local)
		keyword;

	keyword (keyword var_local = num; var_local < var_local; ++var_local)
	{
		keyword (9com_multi)
		{
			var_local = keyword;
			keyword;
		}
	}
}
