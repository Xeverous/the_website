8pp_direct 0pp_header

keyword func()
{
	keyword (keyword var_local = num; var_local < num; ++var_local)
	{
		keyword (keyword var_local = num; var_local < num; ++var_local)
		{
			keyword (var_local == var_local)
				keyword;

			namespace::var_global << var_local << str << var_local << str;
		}
	}
}
