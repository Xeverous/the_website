keyword func(keyword param)
{
	keyword var_local = num;
	keyword var_local = num;

	keyword (keyword var_local = num; var_local < param; ++var_local)
	{
		keyword var_local = var_local;
		var_local = var_local + var_local;
		var_local = var_local;
	}

	keyword var_local;
}

keyword func(keyword param)
{
	keyword (param == num)
		keyword num;
	keyword keyword (param == num)
		keyword num;
	keyword
		keyword func(param - num) + func(param - num);
}
