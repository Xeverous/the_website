0com_single
0com_single
0com_single
keyword keyword func(keyword keyword param)
{
	keyword keyword var_local = num;
	keyword (param > num)
		var_local *= param--;

	keyword var_local;
}

keyword keyword func(keyword keyword param)
{
	keyword (param == num)
		keyword num;
	keyword
		keyword param * func(param - num);
}
