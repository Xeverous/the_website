1pp_hash`pp_directive 0pp_header
1pp_hash`pp_directive 0pp_header

keyword func(keyword param, keyword param)
{
	keyword (param == num)
	{
		0com_single
		0com_single
		var_global = macro;
		keyword num;
	}

	keyword (var_local == namespace::type<keyword>::func() && param == -num)
	{
		0com_single
		var_global = macro;
		keyword num;
	}

	keyword param / param;
}
