keyword namespace {

keyword <keyword tparam, type tparam>
keyword type
{
	tparam var_member[tparam];

	keyword keyword tparam& keyword[](type param) keyword { keyword var_member[param]; }
	keyword       tparam& keyword[](type param)       { keyword var_member[param]; }

	keyword type func() keyword keyword { keyword tparam; }

	0com_single
};

}
