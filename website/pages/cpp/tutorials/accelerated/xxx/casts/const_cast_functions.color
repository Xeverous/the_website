keyword type
{
	keyword var_member = num;

	keyword func(keyword param) keyword
	{
		0com_single
		keyword<type*>(keyword)->var_member = param; 0com_single
	}
};

type var_local;
var_local.func(num); 0com_single

keyword type var_local;
var_local.func(num); 0com_single

keyword (type::* var_local)(keyword) keyword = &type::func;
0com_single
0com_single
